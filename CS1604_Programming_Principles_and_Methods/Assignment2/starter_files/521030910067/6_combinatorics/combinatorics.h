#ifndef COMBINATORICS_H
#define COMBINATORICS_H

// Compute the permutation of n items taken k at a time
int permutations(int n, int k);

// Compute the combination of n items taken k at a time
int combinations(int n, int k);

#endif